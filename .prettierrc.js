module.exports = {
  // Add a semicolon at the end of every statement
  semi: true,
  // Trailing commas wherever possible when multi-line (including function arguments)
  trailingComma: 'all',
  // Use single quotes instead of double quotes
  singleQuote: true,
  // The line length that the printer will wrap on
  printWidth: 100,
  // The number of spaces per indentation-level
  tabWidth: 2,
  // Omit parens around a sole arrow function parameter when possible
  arrowParens: 'avoid',
  // Put the > of a multi-line JSX element alone on the next line
  jsxBracketSameLine: false,
  // Print spaces between brackets in object literals
  bracketSpacing: true,
  // Use single quotes in JSX
  jsxSingleQuote: true,
  // Only add quotes around object properties where required
  quoteProps: 'as-needed',
  //Indent lines with spaces
  useTabs: false,
};
